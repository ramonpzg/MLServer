---
title: 'Alibi-Explain runtime for MLServer'
description: 'MLServer Runtime compatible with the Alibi-Explain library'
---

This package provides a MLServer runtime compatible with Alibi-Explain.

## Usage

You can install the runtime, alongside `mlserver`, as:

```bash
pip install mlserver mlserver-alibi-explain
```
